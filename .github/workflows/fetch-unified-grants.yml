# This workflow has been consolidated into fetch-federal-grants.yml
# to avoid duplication. The unified cache is now built directly in the
# fetch-federal-grants workflow which combines CA + Federal data.
#
# To run: use workflow_dispatch on fetch-federal-grants.yml or wait for scheduled runs.
# This file is kept for reference only.

name: '[DEPRECATED] Fetch Unified Grants Data'

on:
  workflow_dispatch: # Allow manual trigger only

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Show notice
        run: echo "This workflow is deprecated. Use Fetch & Merge Grants Data (fetch-federal-grants.yml) instead."
